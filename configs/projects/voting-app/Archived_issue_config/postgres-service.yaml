apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: demo-voting-app
    name: db-service
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    name: postgres-pod
    app: demo-voting-app
